# Copy this file to docker/.env and edit ROM_PATH.
#
# IMPORTANT:
# Use an absolute path on the host machine.
# This folder is mounted read-only into the backend container as /roms.

ROM_PATH=/Users/davidwork/romm-clone/roms
